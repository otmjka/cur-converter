export type ResultWidgetState = {
  isRateError: boolean;
  result: string;
  amountBase: string;
  base: string;
  baseInQuote: string;
  quote: string;
  quoteSymbol: string;
  quoteInBase: string;
};
